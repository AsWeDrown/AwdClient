#include "LadderHandler.hpp"

namespace awd::game {

    bool LadderHandler::isPassableBy(const Entity& entity) {
        return true;
    }

    bool LadderHandler::isClimbableBy(const Entity& entity) {
        return true;
    }

}
